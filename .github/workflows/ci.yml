name: Continuous Integration of Python Package with Pybind11 Extension

on:
  push:
    branches: [master, main, dev*]
  pull_request:
    branches: [master, main, dev*]
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  build_test:
    name: Build and Test
    strategy:
      matrix:
        # os: [ubuntu-latest, macos-latest, windows-latest]
        os: [windows-latest]
        # python: ["3.9", "3.10"]
        python: ["3.10"]
    runs-on: ${{matrix.os}}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v3
        with:
          python-version: ${{ matrix.python }}
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install build
          python -m pip install pytest
      #     pip install poetry tox tox-gh-actions

      - name: Get latest CMake and ninja
        uses: lukka/get-cmake@latest

      - name: Build Package with Extension
        run: |
          python -m build -w

      - name: Debug Windows wheel
        run: |
          tar -tvf `find dist -name "*.whl"`
      # - name: Test using pytest
      #   run: |
      #     python -m pip install --force-reinstall `find dist -name "*.tar.gz"`
      #     pytest -s -vv

      # - name: Test with tox
      #   run: tox
        
  # publish_testpypi:
  #   needs: build_test